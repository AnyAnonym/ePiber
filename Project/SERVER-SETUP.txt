================================================================================
SERVER-SETUP-ANLEITUNG — ePiber Self-Hosted (Arch Linux + Caddy)
================================================================================
Erstellt: 21.07.2026


================================================================================
1. NEUE PROJEKTSTRUKTUR
================================================================================

Repository-Root (Git):
├── Frontend/                    # Caddy liefert aus (statische Dateien)
│   ├── index.html
│   ├── Bewerbe.html
│   ├── bewerbsRaster.html
│   ├── entryList.html
│   ├── matches.html
│   ├── Matches1.html
│   ├── monitor.html
│   ├── navigator.html
│   ├── players.html
│   ├── preMatches.html
│   ├── rangliste.html
│   ├── RoundRobin.html
│   ├── scoreboard.html
│   ├── scorer-test.html
│   ├── JS/
│   │   ├── SDK.js               ← NICHT im Git! (pro System unterschiedlich)
│   │   ├── SDK.js.example       ← Vorlage im Git
│   │   ├── dataClient.js
│   │   ├── global.js
│   │   ├── clock.js
│   │   ├── footer.js
│   │   ├── navbar.js
│   │   ├── modals.js
│   │   ├── loadingHelper.js
│   │   └── ... (alle weiteren JS-Dateien)
│   └── CSS/
│       ├── styles.css
│       ├── Matches1.css
│       ├── RoundRobin.css
│       └── scoreboard.css
│
├── Backend/                     # Node.js scorer-service
│   ├── server.js
│   ├── config.js                ← liest aus Umgebungsvariablen
│   ├── dataStore.js
│   ├── dataPoller.js
│   ├── courtPoller.js
│   ├── dataProvider.js
│   ├── stateStore.js
│   ├── package.json
│   ├── Dockerfile               ← optional, falls Docker gewünscht
│   ├── .env.example             ← Vorlage im Git
│   └── .env                     ← NICHT im Git! (pro System unterschiedlich)
│
├── Project/                     # Dokumentation, Konfigvorlagen, Serverconfig
│   ├── PROTOKOLL.txt
│   ├── DB.txt
│   ├── Seiten.txt
│   ├── README.md
│   ├── copilot-instructions.md
│   ├── Caddyfile                ← Vorlage für /etc/caddy/Caddyfile
│   ├── systemd/
│   │   ├── epiber-piber.service
│   │   ├── epiber-paj.service
│   │   └── epiber-pk.service
│   └── additional Files/
│       └── ...
│
└── .gitignore


================================================================================
2. SERVER-VERZEICHNISSTRUKTUR
================================================================================

/srv/http/ePiber/
├── piber/                       # LIVE (epiber.at)
│   ├── Frontend/                ← Git checkout
│   ├── Backend/                 ← Git checkout + npm install + .env + service-account.json
│   └── Project/                 ← Git checkout
│
├── paj/                         # TEST PAJ (epiber.at:8081)
│   ├── Frontend/                ← Git checkout
│   ├── Backend/                 ← Git checkout + npm install + .env + service-account.json
│   └── Project/                 ← Git checkout
│
└── pk/                          # TEST PK (epiber.at:8082)
    ├── Frontend/                ← Git checkout
    ├── Backend/                 ← Git checkout + npm install + .env + service-account.json
    └── Project/                 ← Git checkout


================================================================================
3. PORT-ZUORDNUNG
================================================================================

System   | Caddy (extern) | Backend (intern) | WebSocket-URL
---------|----------------|------------------|---------------------------
piber    | 443 (HTTPS)    | 8080             | wss://epiber.at/ws
paj      | 8081           | 8083             | wss://epiber.at:8081/ws
pk       | 8082           | 8084             | wss://epiber.at:8082/ws


================================================================================
4. ERSTEINRICHTUNG AUF DEM SERVER
================================================================================

4.1 Pakete installieren
-----------------------
  pacman -S nodejs npm caddy git

4.2 Benutzer anlegen
--------------------
  useradd -r -s /bin/nologin piber

4.3 Verzeichnisse anlegen
--------------------------
  mkdir -p /srv/http/ePiber/{piber,paj,pk}
  chown -R piber:piber /srv/http/ePiber

4.4 Git-Repository klonen (3x)
-------------------------------
  cd /srv/http/ePiber/piber
  git clone <REPO_URL> .

  cd /srv/http/ePiber/paj
  git clone <REPO_URL> .

  cd /srv/http/ePiber/pk
  git clone <REPO_URL> .

4.5 Backend Dependencies installieren (3x)
-------------------------------------------
  cd /srv/http/ePiber/piber/Backend && npm install --production
  cd /srv/http/ePiber/paj/Backend && npm install --production
  cd /srv/http/ePiber/pk/Backend && npm install --production

4.6 Konfigurationsdateien erstellen (pro System!)
--------------------------------------------------

  # --- LIVE (piber) ---
  # Backend/.env:
  SHEET_ID=1E1CYezDcScIBvH9ebjN0hOkvttTdA6PFIgYKDMaeE04
  PORT=8080
  COURT_URL=https://scorer-tennis.b-cdn.net/json/24.voll.json
  GOOGLE_APPLICATION_CREDENTIALS=/srv/http/ePiber/piber/Backend/service-account.json

  # Frontend/JS/SDK.js:
  export const SCORER_WS_URL = "wss://epiber.at/ws";

  # --- PAJ ---
  # Backend/.env:
  SHEET_ID=1auOvEer7i1PW7LO4QX73188Q81Q6QguGyt0zadIIaqo
  PORT=8083
  COURT_URL=https://scorer-tennis.b-cdn.net/json/24.voll.json
  GOOGLE_APPLICATION_CREDENTIALS=/srv/http/ePiber/paj/Backend/service-account.json

  # Frontend/JS/SDK.js:
  export const SCORER_WS_URL = "wss://epiber.at:8081/ws";

  # --- PK ---
  # Backend/.env:
  SHEET_ID=<PK-Sheet-ID>
  PORT=8084
  COURT_URL=https://scorer-tennis.b-cdn.net/json/24.voll.json
  GOOGLE_APPLICATION_CREDENTIALS=/srv/http/ePiber/pk/Backend/service-account.json

  # Frontend/JS/SDK.js:
  export const SCORER_WS_URL = "wss://epiber.at:8082/ws";

4.7 Google Service-Account-Key
------------------------------
  Den JSON-Key vom Google Cloud Projekt "e-piber" herunterladen:
    Google Cloud Console → IAM → Service Accounts → Keys → Add Key → JSON

  Die Datei als "service-account.json" in jedes Backend/ legen:
    /srv/http/ePiber/piber/Backend/service-account.json
    /srv/http/ePiber/paj/Backend/service-account.json
    /srv/http/ePiber/pk/Backend/service-account.json

  WICHTIG: Der Service Account braucht Bearbeiter-Zugriff auf alle
  3 Google Spreadsheets!

4.8 Caddyfile installieren
---------------------------
  cp /srv/http/ePiber/piber/Project/Caddyfile /etc/caddy/Caddyfile
  systemctl enable --now caddy

4.9 Systemd-Services installieren
----------------------------------
  cp /srv/http/ePiber/piber/Project/systemd/*.service /etc/systemd/system/
  systemctl daemon-reload
  systemctl enable --now epiber-piber
  systemctl enable --now epiber-paj
  systemctl enable --now epiber-pk

4.10 Firewall öffnen
---------------------
  # Ports 443 (HTTPS), 8081, 8082 müssen offen sein
  # Bei iptables:
  iptables -A INPUT -p tcp --dport 443 -j ACCEPT
  iptables -A INPUT -p tcp --dport 8081 -j ACCEPT
  iptables -A INPUT -p tcp --dport 8082 -j ACCEPT

  # Bei firewalld:
  firewall-cmd --permanent --add-port=443/tcp
  firewall-cmd --permanent --add-port=8081/tcp
  firewall-cmd --permanent --add-port=8082/tcp
  firewall-cmd --reload

4.11 DNS
--------
  A-Record: epiber.at → <Server-IP>
  (Caddy holt sich automatisch ein Let's Encrypt Zertifikat)


================================================================================
5. DEPLOYMENT-WORKFLOW (nach Ersteinrichtung)
================================================================================

Entwicklung:
  1. Code ändern (auf Windows in R:\ePiber)
  2. git add + git commit + git push

Server-Update:
  # Live-System
  cd /srv/http/ePiber/piber && git pull
  cd Backend && npm install --production    # nur wenn package.json geändert
  systemctl restart epiber-piber            # nur wenn Backend-Code geändert

  # PAJ-Testsystem
  cd /srv/http/ePiber/paj && git pull
  cd Backend && npm install --production
  systemctl restart epiber-paj

  # PK-Testsystem
  cd /srv/http/ePiber/pk && git pull
  cd Backend && npm install --production
  systemctl restart epiber-pk

  Hinweis: Frontend-Änderungen brauchen keinen Restart —
  Caddy liefert die Dateien direkt aus dem Dateisystem.
  Backend-Änderungen erfordern einen Restart des Services.


================================================================================
6. KONFIGURATIONSDATEIEN-ÜBERSICHT (NICHT im Git)
================================================================================

Pro System unterschiedlich, müssen manuell erstellt werden:

  Backend/.env                    → Sheet-ID, Port, Court-URL, Credentials-Pfad
  Backend/service-account.json    → Google Service Account Key (JSON)
  Frontend/JS/SDK.js              → WebSocket-URL

Vorlagen im Git:
  Backend/.env.example
  Frontend/JS/SDK.js.example


================================================================================
7. STATUS PRÜFEN
================================================================================

  # Systemd-Services
  systemctl status epiber-piber
  systemctl status epiber-paj
  systemctl status epiber-pk

  # Backend Health-Check
  curl https://epiber.at/health
  curl https://epiber.at:8081/health
  curl https://epiber.at:8082/health

  # Backend Detailstatus
  curl https://epiber.at/status

  # Caddy
  systemctl status caddy

  # Logs
  journalctl -u epiber-piber -f
  journalctl -u epiber-paj -f
  journalctl -u caddy -f


================================================================================
8. NICHT MEHR BENÖTIGTE DATEIEN (aus dem alten Projekt)
================================================================================

Die folgenden Dateien/Verzeichnisse aus dem alten Repository werden NICHT
in die neue Struktur übernommen und können gelöscht werden:

  public/                        → ersetzt durch Frontend/
  scorer-service/                → ersetzt durch Backend/
  functions/                     → Firebase Cloud Functions (seit v2.1.0 obsolet)
  firebase.json                  → Firebase Hosting Config (ersetzt durch Caddy)
  firestore.rules                → Firestore nicht mehr genutzt
  firestore.indexes.json         → Firestore nicht mehr genutzt
  .firebaserc                    → Firebase Projektrefernez
  package.json (Root)            → war nur für Firebase
  package-lock.json (Root)       → war nur für Firebase
  .vs/                           → Visual Studio Dateien
