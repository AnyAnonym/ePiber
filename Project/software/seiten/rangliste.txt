================================================================================
Ranglisten-Pyramide - Interaktive Forderungsrangliste
================================================================================
-- Funktion und Dateien --
  HTML: Frontend/rangliste.html
  JS:   Frontend/JS/rangliste.js sowie Profil-/Aktionsmodale in modals.js
  Parameter `?id=<bewerbId>`, Standard aus dem Container oder `2`. Zeigt die
  Rangliste als Pyramide mit Legende und minutenaktualisierten Sperrzeit-Timern.
  Verbindlicher fachlicher Sollstand:
  `Project/Regelwerk/Spielbetrieb/RANGLISTE.txt`.

-- Daten und Aktualisierung --
  Reads: `rlPlatzierung`, minimale `players`-Projektion, `preMatches`, `bewerbe`,
  `readMatchRestrictions` und fuer angemeldete Benutzer
  `rankingChallengeState`. Restriktions- und Eigenstatus muessen vollstaendig
  sein; andernfalls werden Forderungen sichtbar deaktiviert. Invalidierungen
  `ranking`, `matches`, `players`, `bewerbe`, Sessionaenderungen und das naechste
  Restriktionsende serialisieren einen kompletten Refresh. Der voll deckende
  Spinner `Lade Daten ...` bleibt beim Erstaufbau sichtbar, bis Pyramide und alle
  Statusfarben vollstaendig vorbereitet sind. Spaetere Refreshes behalten den
  bisherigen Stand ohne Spinner bis zum atomaren Austausch sichtbar. Initialer
  Erfolg meldet Monitor-Ready, Fehler werden im Inhalt gezeigt und als
  Monitor-Failed gemeldet.

-- Sitzung, Profile und Aktionen --
  Die eigene Position kommt aus der serverseitigen Sitzung, nicht aus Browserdaten.
  Anonym sind Ranglistenboxen reine, nicht fokussierbare Anzeige. Erst nach
  erfolgreicher Anmeldung sind bekannte Personen per Maus und Tastatur als
  Profilaktion bedienbar; ein veralteter oder direkter anonymer Modalaufruf bleibt
  geschlossen. Profilklick verwendet `publicProfile` beziehungsweise fuer das
  eigene Profil `myProfile`. Das gemeinsame Modal zeigt oben in dieser Reihenfolge
  `System`, beim Eigenprofil `Meldungen`, `Aktuell`, `Archiv` und fuer Admins am
  fremden Profil gegebenenfalls `Admin`. Nur `Aktuell` und `Archiv` blenden eine
  zweite, horizontal scrollbar erreichbare Bewerbsreiterzeile ein; eine leere
  Kategorie zeigt `Keine Bewerbe enthalten`. System enthaelt Kontaktwerte, beim
  Eigenprofil Login und Passwortaenderung. Admin enthaelt Passwortfreigabe und
  direktes Passwortsetzen. Die Reiterleisten scrollen horizontal;
  Name und Reiter bleiben fest, nur der auf Viewport und Safe Area begrenzte Inhalt
  scrollt vertikal. Hintergrundscrollen ist waehrend des Dialogs gesperrt, Escape
  schliesst ihn.
  Ranglisten- und Matchdaten desselben Bewerbs werden zusammengefuehrt. Dadurch
  erscheinen alle offenen und historischen abgeschlossenen Matches als Karten.
  Echte offene Ranglistenbegegnungen stehen vor den abgeschlossenen Karten;
  innerhalb beider Gruppen bleibt `MatchDate` absteigend sortiert, Freilose
  erhalten keinen offenen Vorrang. Ergebnisaktionen und -dialog entsprechen
  vollstaendig `Project/software/seiten/players.txt` und sind keine eigene
  Bedienfunktion der Ranglistenseite.
  Ein aktiver Ranglistenreiter zeigt `Ranglistenposition: <Rang>` und bei einer
  offenen Forderung nur Forderungsdatum und Terminfrist. Gegner, Runde und
  Spieltermin stehen ausschliesslich in der zugehoerigen Matchkarte. Beim eigenen
  Profil erscheint `Raushängen` nur, wenn die serverseitige Projektion
  `canWithdraw` erlaubt; bei einer offenen Forderung fehlen Aktion und
  Sperrhinweis. Bei einem tatsaechlich forderbaren fremden Profil erscheint
  `Fordern`. Rang `0` bleibt aus der Pyramide entfernt, besitzt aber einen
  Profilreiter mit Raushaengezeit und Grund fuer angemeldete Benutzer; eine
  Position `0` wird nicht angezeigt. Das Eigenprofil liefert den gespeicherten
  letzten Rang als Startpunkt fuer eine Rueckkehrforderung.
  Forderungen senden stabile `operationId`, `bewerbId` und `opponentId`; der Server
  prueft Identitaet und Regeln. Bei Erfolg ist die Forderung sofort verbindlich
  und wird mit dem Forderer in `Spieler1ID` sowie dem Geforderten in `Spieler3ID`
  gespeichert. Bestehende Matchzeilen werden nicht umgeordnet.
  Der Eigenprofilreiter `Meldungen` zeigt jede Meldung als Karte in der Zeilenfolge
  `<Datum>`, `<Bewerb> - <Runde>`, `<Meldungstext>` und bei bekanntem Akteur
  `Eingetragen durch: <Name>`. Ohne Runde entfallen Runde und Trennstrich; ohne
  Zuordnung lautet die kontrollierte Bewerbszeile `Allgemeine Meldung`.
  Ranglistenmeldungen enthalten keine Runde. Datum, Bewerb, Runde und Meldungstext
  sind gleich gross, nur der Meldungstext ist fett und die Akteurszeile eine
  Schriftstufe kleiner. Die Detailansicht ordnet Betreff, Datum, Bewerb, bei
  bekanntem Akteur `Eingetragen durch: <Name>` und danach den Text an; eine Runde
  wird dort nicht wiederholt. Nach erfolgreicher Kenntnisnahme sowie bei bereits
  bestaetigten Meldungen wird die Aktion ausgeblendet; serverseitige
  Bestaetigungszeit, Ungelesenanzahl und Revision bleiben autoritativ.
  Navigation und Profilreiter zeigen die Ungelesenanzahl; auf Mobilgeraeten
  werden bei ungelesenen Meldungen die Hamburger-Striche rot. Der Systemreiter
  weist die administrativ gepflegten gueltigen Kanaele Email und Whatsapp aus.
  Meldungen werden ungelesen vor gelesen und je Gruppe neueste zuerst sortiert.
  Persoenliche Forderungsmeldungen nennen fuer Forderer und Geforderten die beim
  Anlegen fixierten Rangpositionen in Klammern; diese Texte werden bei spaeteren
  Namens- oder Rangaenderungen nicht aus aktuellen Daten neu erzeugt.
  Auf allen Viewports oeffnet die gemeinsame Navigation als rechtsseitiger,
  intern scrollbarer Drawer ab Bildschirmoberkante mit `min(82vw, 420px)` Breite
  und voller Hoehe und
  schiebt App samt Header nach links. Linke Restflaeche, X und Escape schliessen
  ihn. Die Kopfzeile `ePiber` besitzt Headerhoehe. Linksbuendige Listenzeilen mit
  Icons und Trennlinien ordnen Profil, einen nur bei Inhalt sichtbaren
  Favoritenbereich, Dashboard und die zunaechst geschlossenen Gruppen
  `Spielbetrieb`, `Verein` und `Administration`; die rote Abmeldezeile steht am
  Ende. Anonym ersetzt eine gruene Anmeldezeile das Profil. Desktop, Tablet und
  Mobilgeraete verwenden denselben Hamburger und Drawer.
  Ergebnis- und Korrekturmeldungen sprechen Gewinner und Verlierer jeweils
  empfaengerbezogen an und nennen die gegnerische Seite. Bei W.O. lautet der
  Gewinnsatz `Du gewinnst durch W.O. von <Verliererseite>.`, fuer die selbst nicht
  angetretene Seite `Du verlierst durch W.O.`. Bei Aufgabe folgt `Ergebnis:
  <Spielstand> (Aufgabe).`; ohne Teilergebnis wird `Ergebnis: (Aufgabe).`
  angezeigt. Die Bewerbshistorie nennt beide Seiten bei W.O. neutral als
  `<Gewinnerseite> gewinnt durch W.O. von <Verliererseite>.` und zeigt darunter
  `Ergebnis: W.O.`. Der mit `Eingetragen durch:` bezeichnete Akteur bleibt
  fachlich vom W.O.-Geber getrennt.

  Ein Profil-Ranglistenreiter mit einer offenen Forderung samt vorhandenem
  Forderungsdatum zwischen zwei unterschiedlichen, eindeutig zugeordneten
  Hauptspielern zeigt Administratoren die beiden roten Korrekturaktionen
  `Forderung löschen` und `Forderungsdatum ändern`. Das gilt auch fuer
  Neueinsteiger ohne Ranglistenzeile; ihr Reiter besitzt keine Positionsanzeige.
  Beide Aktionen verlangen einen getrimmten Grund mit 1 bis 500 Zeichen. Das
  Forderungsdatum darf minutengenau im darstellbaren Bereich 1950 bis 2049 ohne
  weitere Vergangenheits-, Zukunfts- oder Reihenfolgegrenze korrigiert werden.
  Die Spielterminpflege erfolgt dagegen ueber die gemeinsame Matchkartenaktion
  nach den unten beschriebenen Terminregeln. Nach Erfolg oeffnen sich dasselbe
  Profil, dieselbe Kategorie und derselbe Bewerbsreiter erneut. Eine
  Loeschmeldung lautet fuer den Forderer
  `Forderung gegen <Gegner> gelöscht` und fuer den Geforderten `Forderung von
  <Forderer> gelöscht`; der Pflichtgrund ist gezielt in der Bewerbshistorie
  sichtbar, der Administrator bleibt als Akteur getrennt ausgewiesen.

  Die gemeinsame Matchkarte zeigt den Beteiligten bei leerem MatchDate `Termin
  eintragen`, danach `Termin abändern`. Der Dialog waehlt
  Datum und volle Stunde getrennt; `Übernehmen` sendet den idempotenten Write.
  Der erste Termin liegt im Korridor vom Forderungszeitpunkt bis exakt vierzehn
  Tage danach. Spaetere Aenderungen duerfen ab der aktuellen Serverzeit ohne
  obere Datumsgrenze liegen. Das Stundenfeld bietet 06:00 bis 23:00 Uhr.
  Der Monatskalender kann unbegrenzt vor- und zurueck navigiert werden: Der
  Forderungstag ist blau und fett, die Sieben-Tage-Terminfrist orange und der
  letzte Korridortag rot und fett, alle
  Korridortage sind fett und auswaehlbar; andere Tage sind ausgegraut und nicht
  auswaehlbar. Oberhalb der Verfuegbarkeitsmeldung bezeichnet eine farbige Legende
  Forderungszeitpunkt, Terminfrist und Spielschluss jeweils mit Datum. Die Werte
  stammen aus dem gemeinsamen serverseitigen `scheduleMarkers`-Vertrag, den
  spaetere Bewerbsrundenlimits ohne Aenderung des Dialogs ebenfalls verwenden
  koennen. Fehler lassen den Dialog offen, zeigen die kontrollierte Meldung
  samt Support-ID und erzeugen nur das allowlist-basierte Browserereignis
  `match_date_action_failed`.

  Solange kein Termin gesetzt ist, zeigt der eigene Ranglistenreiter sowie bei
  Administratoren der fremde Ranglistenreiter einen minuetlich aktualisierten
  Countdown bis sieben Tage nach Forderung. Vor der Frist steht `-`, nachher `+`;
  bis zwei Tage Restzeit ist er gruen, ab zwei Tagen orange und nach
  Ueberschreitung rot. Terminsetzung oder Schliessen der Forderung beendet
  diesen temporaeren Zustand.

-- Fachlogik --
  Blau: eigene Position; gruen: forderbar; gelb: offene Forderung; rosa: Schonzeit;
  lila: Sperrzeit. Eine offene Forderung hat auch fuer den angemeldeten Spieler
  Vorrang vor einer noch laufenden Schon- oder Sperrzeit: Die Box ist gelb, behaelt
  ihren blauen Eigenrahmen und zeigt keinen Fristtimer. Forderbar sind links in derselben Reihe und ab eigener Spalte in
  der Reihe darueber; Rang 3 darf Rang 1 fordern. Eigene/offene/geschuetzte oder
  gesperrte Konstellationen begrenzen Aktionen. Fuer einen rausgehaengten Spieler
  werden der gespeicherte letzte Rang und alle dahinterliegenden Raenge gruen
  dargestellt; der Server prueft dieselbe Untergrenze und die Zwoelfmonatsfrist.
  Ein nach Geschlecht und Kalenderjahr-Altersgrenze teilnahmeberechtigter
  Neueinsteiger ohne Ranglistenzeile sowie ein Spieler nach Ablauf seiner
  Rueckkehrfrist sieht alle regulaer verfuegbaren Gegner gruen und kann jeden
  davon fordern. Fehlende eingeschraenkte Personendaten und unvollstaendige
  Regeldaten bleiben fail-closed.
  Ein Match mit dem exakt
  kleingeschriebenen Marker `[wo]` oder `[ret]` gilt auch ohne Ergebnis als
  abgeschlossen und wird nicht mehr gelb als offene Forderung angezeigt. Gewinner
  und Verlierer erhalten die serverseitig berechnete Schon- beziehungsweise
  Sperrzeit samt Timer; beim eigenen Spieler bleibt der blaue Rahmen zusaetzlich
  zur Fristdarstellung sichtbar. Andere Markerschreibweisen werden nicht erkannt.
  Eine angelegte Forderung kann nicht abgelehnt oder abgebrochen werden. Ein
  regelwidriger oder technisch fehlgeschlagener Aufruf legt keine Forderung an;
  ein spaeterer Nichtantritt wird als Walkover abgeschlossen.

-- Observability --
  Jeder Forderungs-, Termin- und Raushaengewrite besitzt Audit `started` und danach `success`, `failed`
  oder `unknown`. Der Forderer stammt aus der Session, Zielperson und Bewerb aus
  validierten Parametern; Erfolg ergaenzt die stabile Match-ID. Der kontrollierte
  Journalspiegel erlaubt das Filtern im Grafana-Dashboard
  `ePiber Ranglistenaktivitaeten`. Personen-, Bewerbs-, Match-, Request- und
  Operation-IDs bleiben JSON-Felder und werden keine Loki-Labels. Das
  Raushaengeaudit enthaelt Bewerb, alten Rang, Zeitpunkt und Anzahl der
  Rangverschiebungen, aber nie den freien Grund.
  Das Terminaudit enthaelt Match-ID und kontrollierten alten/neuen Termin; das
  Abschlusslog ergaenzt Bewerbs- und Akteur-ID sowie Recoverystatus, aber keine
  freien Texte oder unnoetigen Personendaten.
  Ergebniswrites verwenden die Auditaktionen `setMatchResult`,
  `adminSetMatchEnd`, `adminClearMatchResult` und `adminCorrectRankingResult` mit
  `started` sowie `success|failed|unknown`. Der Browser meldet nur die allowlist-
  basierten Events `match_result_action_failed` und
  `match_result_suggestion_failed`; freie Ergebnisse und Gruende bleiben
  ausgeschlossen.
  Fehler der beiden administrativen Forderungskorrekturen verwenden
  `ranking_admin_action_failed`. Audit und Abschlusslogs enthalten kontrollierte
  Aktions-, Bewerbs-, Match-, Akteur- und Datumsfelder sowie Recovery und das
  Vorhandensein eines Grundes, aber weder den freien Grundtext noch Matchpayloads.
  Die gemeinsame administrative Terminaktion verwendet stattdessen
  `match_date_action_failed` und den generischen Termin-Auditvertrag.

-- Raushaengen und Sicherheit --
  Der Bestaetigungsdialog verlangt ausschliesslich einen Grund mit 3 bis 500
  Zeichen. Der Write wird waehrend der Anfrage gegen Schliessen gesperrt.
  Nachfolgende aktive Raenge ruecken in einem Batch auf;
  unklare Google-Ausgaenge werden anhand eines persistenten Plans bestaetigt statt
  blind wiederholt. Angemeldete Benutzer koennen ueber den Button
  `Rausgehängte Spieler` bei der Legende die nach Zeitpunkt absteigend sortierte
  Liste mit Datum und Grund oeffnen. Namen, Gruende und Pyramide entstehen als
  sichere DOM-Textknoten.

  Ein Ergebnis fuehrt die Regeln fuer gereihte Forderer, Neueinsteiger und
  Rueckkehrer automatisch im selben technischen Plan aus. Korrekturen durch
  Beteiligte setzen unveraenderten Rangprovenienzstand voraus; andernfalls kann
  nur ein Administrator mit Pflichtgrund und vollstaendigem Rangplan korrigieren.
  Darin koennen aktive Mitglieder nur positive Zielraenge erhalten. Rang 0 bleibt
  bereits rausgehaengten Mitgliedern vorbehalten und darf fuer diese mehrfach
  vorkommen; ihre Aktivierung auf einen positiven Rang ist zulaessig.
  Eine administrative Ergebnisloeschung stellt bei unveraenderter Provenienz den
  vorherigen Rangstand her und entfernt danach die Provenienz.
